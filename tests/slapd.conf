include         /etc/ldap/schema/core.schema
include         ~/docs/ldapstore.ldif

pidfile         /tmp/slapd/slapd.pid
argsfile        /tmp/slapd/slapd.args

database        ldif
suffix          "dc=example,dc=com"
rootdn          "cn=admin,dc=example,dc=com"

rootpw          secret

directory       /tmp/slapd

index           objectClass     eq
